import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yunjin.tundercrushapp',
  appName: 'TunderCrush app ',
  webDir: 'www'
};

export default config;
